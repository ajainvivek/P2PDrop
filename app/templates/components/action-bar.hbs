{{#liquid-if isFileSelected class="action-bar"}}
  <div class="container">
    <button type="button" class="btn btn-primary">Preview</button>
    <a href="{{url}}" download="{{name}}" type="button" class="btn btn-primary">Download</a>
  </div>
{{else}}
<div class="container">
  <h1>{{localIp}}</h1>
</div>
{{/liquid-if}}
